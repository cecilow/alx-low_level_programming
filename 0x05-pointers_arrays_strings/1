#include "main.h"
/**
 * print_rev - print reverse
 * @s: string
 * Return: return zero
 */
void print_rev(char *s)
{
	int i;

	for (i = _strlen(s); s[i] != '\0'; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
